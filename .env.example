# Default local: sqlite — leave MYNA_STORAGE unset
# MYNA_STORAGE=sqlite

# Cloudflare deploy uses D1 via wrangler.toml (MYNA_STORAGE=d1 + DB binding)

# Optional Turso instead of D1
# MYNA_STORAGE=turso
# TURSO_DATABASE_URL=libsql://<db>-<org>.turso.io
# TURSO_AUTH_TOKEN=

# Shared secret (optional) — protects ingest + webhook writes.
# Web: VITE_MYNA_SECRET / 设置页「密钥」；SDK: initMyna({ token })
# MYNA_SECRET=dev-secret

# PORT=8787
